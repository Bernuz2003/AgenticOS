{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "agenticos.control.delete_job.v1",
  "title": "AgenticOS DELETE_JOB Response v1",
  "allOf": [
    {"$ref": "./control-envelope.schema.json"},
    {
      "properties": {
        "schema_id": {"const": "agenticos.control.delete_job.v1"},
        "data": {
          "type": "object",
          "properties": {
            "job_id": {"type": "integer", "minimum": 1},
            "enabled": {"const": false},
            "state": {"const": "deleted"}
          },
          "required": ["job_id", "enabled", "state"]
        }
      }
    }
  ]
}
