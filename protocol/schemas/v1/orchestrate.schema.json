{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "agenticos.control.orchestrate.v1",
  "title": "AgenticOS ORCHESTRATE Response v1",
  "allOf": [
    {"$ref": "./control-envelope.schema.json"},
    {
      "properties": {
        "schema_id": {"const": "agenticos.control.orchestrate.v1"},
        "data": {
          "type": "object",
          "properties": {
            "orchestration_id": {"type": "integer", "minimum": 1},
            "total_tasks": {"type": "integer", "minimum": 0},
            "spawned": {"type": "integer", "minimum": 0}
          },
          "required": ["orchestration_id", "total_tasks", "spawned"]
        }
      }
    }
  ]
}
