{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "agenticos.control.ping.v1",
  "title": "AgenticOS PING Response v1",
  "allOf": [
    {"$ref": "./control-envelope.schema.json"},
    {
      "properties": {
        "schema_id": {"const": "agenticos.control.ping.v1"},
        "data": {
          "type": "object",
          "required": ["message"],
          "properties": {"message": {"const": "PONG"}},
          "additionalProperties": false
        }
      }
    }
  ]
}
