{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "agenticos.control.stop_output.v1",
  "title": "AgenticOS Stop Output Response v1",
  "allOf": [
    {"$ref": "./control-envelope.schema.json"},
    {
      "properties": {
        "schema_id": {"enum": ["agenticos.control.stop_output.v1"]},
        "data": {
          "type": "object",
          "required": ["pid", "state", "action"],
          "properties": {
            "pid": {"type": "integer", "minimum": 0},
            "state": {"type": "string"},
            "action": {"type": "string", "enum": ["stop_output"]}
          },
          "additionalProperties": false
        }
      }
    }
  ]
}
